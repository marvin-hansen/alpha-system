load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "db_pg_smdb_instrument_tests",
    srcs = glob([
        "service_tests.rs",
    ]),
    tags = [
        "db_smdb_tests",
        "integration_test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:pg_smdb",
        # Internal crates
        "//alias:common_config",
        "//alias:env_utils",
        "//alias:db_specs",
        # External crates
        "@crates//:tokio",
        "@crates//:diesel",
        "@crates//:dotenv",
        "@crates//:serde",
    ],
)
