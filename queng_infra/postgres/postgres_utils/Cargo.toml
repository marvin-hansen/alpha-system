[package]
name = "postgres_utils"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
readme.workspace = true
repository.workspace = true
authors.workspace = true


[dependencies]
# Internal crates
common_config = {workspace = true}
common_exchange = {workspace = true}
common_pg_queries = {workspace = true}
container_specs = {workspace = true}
docker_utils = {workspace = true}
# External crates
postgres-types = { workspace = true }
tokio-postgres = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
smdb_specs = {workspace = true} # For testing