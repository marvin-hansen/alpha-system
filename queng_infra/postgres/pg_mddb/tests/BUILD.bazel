load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "tests",
    srcs = glob([
        "asset/asset_type_conversion_tests.rs",
        "exchange/exchange_type_conversion_tests.rs",
        "instrument/instrument_type_conversion_tests.rs",
        "stat/stat_type_conversion_tests.rs",
    ]),
    tags = [
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:pg_mddb",
        # Internal crates
        "//alias:common_metadata",
        "//alias:postgres_migrations",
        # External crates
        "//thirdparty/crates:bon",
        "//thirdparty/crates:diesel",
        "//thirdparty/crates:diesel_migrations",
    ],
)
