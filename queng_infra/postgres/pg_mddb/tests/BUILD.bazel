load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "tests",
    srcs = glob([
        "type_conversion/*_tests.rs",
    ]),
    tags = [
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:pg_mddb",
        # Internal crates
        "//alias:common_metadata",
        "//alias:container_specs_postgres",
        "//alias:postgres_migrations",
        # External crates
        "//thirdparty/crates:bon",
        "@crates//:diesel",
        "@crates//:diesel_migrations",
    ],
)

rust_test_suite(
    name = "asset",
    srcs = glob([
        "asset/*_tests.rs",
    ]),
    exec_properties = {
        # Tell BuildBuddy to run this test using a Firecracker microVM.
        "test.workload-isolation-type": "firecracker",
        # Tell BuildBuddy to ensure that the Docker daemon is started
        "test.init-dockerd": "true",
        # Tell BuildBuddy to preserve the microVM state across test runs.
        "test.recycle-runner": "true",
        # We don't want different different db tests to be assigned to the same recycled runner,
        # because we can't fit all db docker images with the default disk limit.
        "test.runner-recycling-key": "pg_metadb_asset_tests",
    },
    tags = [
        "integration_test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:pg_mddb",
        # Internal crates
        "//alias:common_metadata",
        "//alias:container_specs_postgres",
        "//alias:postgres_migrations",
        "//alias:docker_utils",
        # External crates
        "@crates//:diesel",
        "@crates//:diesel_migrations",
        "//thirdparty/crates:tokio",
    ],
)

rust_test_suite(
    name = "exchange",
    srcs = glob([
        "exchange/*_tests.rs",
    ]),
    exec_properties = {
        # Tell BuildBuddy to run this test using a Firecracker microVM.
        "test.workload-isolation-type": "firecracker",
        # Tell BuildBuddy to ensure that the Docker daemon is started
        "test.init-dockerd": "true",
        # Tell BuildBuddy to preserve the microVM state across test runs.
        "test.recycle-runner": "true",
        # We don't want different different db tests to be assigned to the same recycled runner,
        # because we can't fit all db docker images with the default disk limit.
        "test.runner-recycling-key": "pg_metadb_exchange_tests",
    },
    tags = [
        "integration_test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:pg_mddb",
        # Internal crates
        "//alias:common_metadata",
        "//alias:container_specs_postgres",
        "//alias:postgres_migrations",
        "//alias:docker_utils",
        # External crates
        "@crates//:diesel",
        "@crates//:diesel_migrations",
        "//thirdparty/crates:tokio",
    ],
)

rust_test_suite(
    name = "instrument",
    srcs = glob([
        "instrument/*_tests.rs",
    ]),
    exec_properties = {
        # Tell BuildBuddy to run this test using a Firecracker microVM.
        "test.workload-isolation-type": "firecracker",
        # Tell BuildBuddy to ensure that the Docker daemon is started
        "test.init-dockerd": "true",
        # Tell BuildBuddy to preserve the microVM state across test runs.
        "test.recycle-runner": "true",
        # We don't want different different db tests to be assigned to the same recycled runner,
        # because we can't fit all db docker images with the default disk limit.
        "test.runner-recycling-key": "pg_metadb_instrument_tests",
    },
    tags = [
        "integration_test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:pg_mddb",
        # Internal crates
        "//alias:common_metadata",
        "//alias:container_specs_postgres",
        "//alias:postgres_migrations",
        "//alias:docker_utils",
        # External crates
        "@crates//:diesel",
        "@crates//:diesel_migrations",
        "//thirdparty/crates:tokio",
    ],
)

rust_test_suite(
    name = "instruments_exchanges",
    srcs = glob([
        "instruments_exchanges/*_tests.rs",
    ]),
    exec_properties = {
        # Tell BuildBuddy to run this test using a Firecracker microVM.
        "test.workload-isolation-type": "firecracker",
        # Tell BuildBuddy to ensure that the Docker daemon is started
        "test.init-dockerd": "true",
        # Tell BuildBuddy to preserve the microVM state across test runs.
        "test.recycle-runner": "true",
        # We don't want different different db tests to be assigned to the same recycled runner,
        # because we can't fit all db docker images with the default disk limit.
        "test.runner-recycling-key": "pg_metadb_instruments_exchanges_tests",
    },
    tags = [
        "integration_test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:pg_mddb",
        # Internal crates
        "//alias:common_metadata",
        "//alias:container_specs_postgres",
        "//alias:postgres_migrations",
        "//alias:docker_utils",
        # External crates
        "@crates//:diesel",
        "@crates//:diesel_migrations",
        "//thirdparty/crates:tokio",
    ],
)

rust_test_suite(
    name = "stat",
    srcs = glob([
        "stat/*_tests.rs",
    ]),
    exec_properties = {
        # Tell BuildBuddy to run this test using a Firecracker microVM.
        "test.workload-isolation-type": "firecracker",
        # Tell BuildBuddy to ensure that the Docker daemon is started
        "test.init-dockerd": "true",
        # Tell BuildBuddy to preserve the microVM state across test runs.
        "test.recycle-runner": "true",
        # We don't want different different db tests to be assigned to the same recycled runner,
        # because we can't fit all db docker images with the default disk limit.
        "test.runner-recycling-key": "pg_metadb_stat_tests",
    },
    tags = [
        "integration_test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:pg_mddb",
        # Internal crates
        "//alias:common_metadata",
        "//alias:container_specs_postgres",
        "//alias:postgres_migrations",
        "//alias:docker_utils",
        # External crates
        "@crates//:diesel",
        "@crates//:diesel_migrations",
        "//thirdparty/crates:tokio",
    ],
)
