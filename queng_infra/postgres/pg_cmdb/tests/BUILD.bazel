load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "pg_cmdb_instrument_tests",
    srcs = glob([
        "instrument/*_tests.rs",
        "setup/cmdb_setup.rs",
    ]),
    exec_properties = {
        # Tell BuildBuddy to run this test using a Firecracker microVM.
        "test.workload-isolation-type": "firecracker",
        # Tell BuildBuddy to ensure that the Docker daemon is started
        "test.init-dockerd": "true",
        # Tell BuildBuddy to preserve the microVM state across test runs.
        "test.recycle-runner": "true",
        # We don't want different different db tests to be assigned to the same recycled runner,
        # because we can't fit all db docker images with the default disk limit.
        "test.runner-recycling-key": "pg_cmdb_instrument_tests",
    },
    tags = [
        "integration_test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:pg_cmdb",
        # Internal crates
        "//alias:common_database",
        "//alias:common_exchange",
        "//alias:postgres_test_utils",
        # External crates
        "//thirdparty/crates:tokio",
        "//thirdparty/crates:diesel",
    ],
)

rust_test_suite(
    name = "pg_cmdb_portfolio_tests",
    srcs = glob([
        "portfolio/portfolio_tests.rs",
        "portfolio_instrument/portfolio_instrument_tests.rs",
        "setup/cmdb_setup.rs",
    ]),
    exec_properties = {
        # Tell BuildBuddy to run this test using a Firecracker microVM.
        "test.workload-isolation-type": "firecracker",
        # Tell BuildBuddy to ensure that the Docker daemon is started
        "test.init-dockerd": "true",
        # Tell BuildBuddy to preserve the microVM state across test runs.
        "test.recycle-runner": "true",
        # We don't want different different db tests to be assigned to the same recycled runner,
        # because we can't fit all db docker images with the default disk limit.
        "test.runner-recycling-key": "cmdb_portfolio_tests",
    },
    tags = [
        # "integration_test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:pg_cmdb",
        # Internal crates
        "//alias:common_database",
        "//alias:common_exchange",
        "//alias:postgres_test_utils",
        # External crates
        "//thirdparty/crates:tokio",
        "//thirdparty/crates:diesel",
    ],
)
