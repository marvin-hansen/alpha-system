load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "env_container_setup_tests",
    size = "medium",
    srcs = glob([
        "env/containers/container_setup_tests.rs",
    ]),
    exec_properties = {
        # Tell BuildBuddy to run this test using a Firecracker microVM.
        "test.workload-isolation-type": "firecracker",
        # Tell BuildBuddy to ensure that the Docker daemon is started
        "test.init-dockerd": "true",
        # Tell BuildBuddy to preserve the microVM state across test runs.
        "test.recycle-runner": "true",
        # We don't want different different db tests to be assigned to the same recycled runner,
        # because we can't fit all db docker images with the default disk limit.
        "test.runner-recycling-key": "container",
    },
    tags = ["env_container_setup"],
    visibility = ["//visibility:public"],
    deps = [
        # Crate under test
        "//alias:env_utils",
        # Internal crates
        "//alias:clickhouse_utils",
        "//alias:docker_utils",
        "//alias:kaiko_utils",
        # External crates
        "//thirdparty/crates:tokio",
        "//thirdparty/crates:serde_json",
    ],
)

rust_test_suite(
    name = "env_ci_setup_tests",
    size = "medium",
    srcs = glob([
        "env/ci/env_ci_setup_tests.rs",
    ]),
    exec_properties = {
        # Tell BuildBuddy to run this test using a Firecracker microVM.
        "test.workload-isolation-type": "firecracker",
        # Tell BuildBuddy to ensure that the Docker daemon is started
        "test.init-dockerd": "true",
        # Tell BuildBuddy to preserve the microVM state across test runs.
        "test.recycle-runner": "true",
        # We don't want different different db tests to be assigned to the same recycled runner,
        # because we can't fit all db docker images with the default disk limit.
        "test.runner-recycling-key": "container",
    },
    tags = ["env_ci_setup"],
    visibility = ["//visibility:public"],
    deps = [
        # Crate under test
        "//alias:env_utils",
        # Internal crates
        "//alias:clickhouse_utils",
        "//alias:docker_utils",
        "//alias:kaiko_utils",
        # External crates
        "//thirdparty/crates:tokio",
        "//thirdparty/crates:serde_json",
    ],
)

rust_test_suite(
    name = "env_ci_teardown_tests",
    size = "small",
    timeout = "short",
    srcs = glob([
        "env/ci/env_ci_teardown_tests.rs",
    ]),
    exec_properties = {
        # Tell BuildBuddy to run this test using a Firecracker microVM.
        "test.workload-isolation-type": "firecracker",
        # Tell BuildBuddy to ensure that the Docker daemon is started
        "test.init-dockerd": "true",
        # Tell BuildBuddy to preserve the microVM state across test runs.
        "test.recycle-runner": "true",
        # We don't want different different db tests to be assigned to the same recycled runner,
        # because we can't fit all db docker images with the default disk limit.
        "test.runner-recycling-key": "container",
    },
    tags = ["env_ci_teardown"],
    visibility = ["//visibility:public"],
    deps = [
        # Crate under test
        "//alias:env_utils",
        # Internal crates
        "//alias:clickhouse_utils",
        "//alias:docker_utils",
        # External crates
        "//thirdparty/crates:tokio",
        "//thirdparty/crates:serde_json",
    ],
)

rust_test_suite(
    name = "env_container_teardown_tests",
    size = "small",
    timeout = "short",
    srcs = glob([
        "env/containers/container_teardown_tests.rs",
    ]),
    exec_properties = {
        # Tell BuildBuddy to run this test using a Firecracker microVM.
        "test.workload-isolation-type": "firecracker",
        # Tell BuildBuddy to ensure that the Docker daemon is started
        "test.init-dockerd": "true",
        # Tell BuildBuddy to preserve the microVM state across test runs.
        "test.recycle-runner": "true",
        # We don't want different different db tests to be assigned to the same recycled runner,
        # because we can't fit all db docker images with the default disk limit.
        "test.runner-recycling-key": "container",
    },
    tags = ["env_container_teardown"],
    visibility = ["//visibility:public"],
    deps = [
        # Crate under test
        "//alias:env_utils",
        # Internal crates
        "//alias:clickhouse_utils",
        "//alias:docker_utils",
        # External crates
        "//thirdparty/crates:tokio",
        "//thirdparty/crates:serde_json",
    ],
)
