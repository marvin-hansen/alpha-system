load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "ims_common_binance_type_tests",
    srcs = glob([
        "tests/types/binance_types/*_tests.rs",
    ]),
    tags = [
        "binance_types",
        "ims_common",
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        ":common_data_bar",
        # Internal crates

        # External crates
        "@crates//:serde",
    ],
)
