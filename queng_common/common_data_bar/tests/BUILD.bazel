load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "data",
    srcs = glob([
        "data_types/*_tests.rs",
    ]),
    tags = [
        "common",
        "unit",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:common_data_bar",
    ],
)
