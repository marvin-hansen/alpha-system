load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "mod",
    srcs = ["mod.rs"],
)

rust_library(
    name = "portfolio_config",
    srcs = ["portfolio_config.rs"],
    deps = ["@crates//:serde"],
)
