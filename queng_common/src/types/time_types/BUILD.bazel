load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "mod",
    srcs = ["mod.rs"],
)

rust_library(
    name = "month",
    srcs = ["month.rs"],
    deps = ["@crates//:serde"],
)

rust_library(
    name = "time_scale",
    srcs = ["time_scale.rs"],
)
