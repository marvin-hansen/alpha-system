load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "mod",
    srcs = ["mod.rs"],
)

rust_library(
    name = "symbol",
    srcs = ["symbol.rs"],
    deps = [
        "@crates//:rust_decimal",
        "@crates//:serde",
    ],
)

rust_library(
    name = "symbol_id",
    srcs = ["symbol_id.rs"],
    deps = ["@crates//:serde"],
)
