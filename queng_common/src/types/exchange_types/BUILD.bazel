load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "account_type",
    srcs = ["account_type.rs"],
    deps = ["@crates//:serde"],
)

rust_library(
    name = "exchange_id",
    srcs = ["exchange_id.rs"],
    deps = ["@crates//:serde"],
)

rust_library(
    name = "mod",
    srcs = ["mod.rs"],
)

rust_library(
    name = "security_type",
    srcs = ["security_type.rs"],
    deps = ["@crates//:serde"],
)
