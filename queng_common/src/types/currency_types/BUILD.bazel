load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "fiat_iso_code",
    srcs = ["fiat_iso_code.rs"],
    deps = ["@crates//:serde"],
)

rust_library(
    name = "mod",
    srcs = ["mod.rs"],
)
