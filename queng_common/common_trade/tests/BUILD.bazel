load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "trade",
    srcs = glob([
        "trade_types/*_tests.rs",
    ]),
    tags = [
        "common",
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:common_trade",
    ],
)
