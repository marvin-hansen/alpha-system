load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "mod",
    srcs = ["mod.rs"],
)

rust_library(
    name = "trade_bar_tests",
    srcs = ["trade_bar_tests.rs"],
    deps = [
        "@crates//:chrono",
        "@crates//:rust_decimal",
    ],
)

rust_test(
    name = "trade_bar_tests_test",
    crate = ":trade_bar_tests",
)
