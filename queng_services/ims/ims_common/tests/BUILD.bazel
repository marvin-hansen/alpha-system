load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "ims_common_binance_type_tests",
    srcs = glob([
        "types/binance_types/*_tests.rs",
    ]),
    tags = [
        "binance_types",
        "ims_common",
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias/service:ims_common",
        # Internal crates
        "//alias:common_data_bar",
        # External crates
        "//thirdparty/crates:serde",
    ],
)
