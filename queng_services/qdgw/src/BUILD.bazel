load("@rules_rust//rust:defs.bzl", "rust_binary", "rust_library")

rust_binary(
    name = "main",
    srcs = ["main.rs"],
    deps = [
        "@crates//:autometrics",
        "@crates//:futures",
        "@crates//:tokio",
        "@crates//:warp",
    ],
)

rust_library(
    name = "service",
    srcs = ["service.rs"],
    deps = [
        "@crates//:futures",
        "@crates//:tokio",
    ],
)
