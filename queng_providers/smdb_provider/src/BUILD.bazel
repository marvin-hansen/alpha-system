load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "lib",
    srcs = ["lib.rs"],
    deps = [
        "@crates//:proto",
        "@crates//:tonic",
    ],
)

rust_library(
    name = "prv_smdb",
    srcs = ["prv_smdb.rs"],
    deps = [
        "//queng_common/src:common",
        "@crates//:proto",
        "@crates//:tonic",
    ],
)
