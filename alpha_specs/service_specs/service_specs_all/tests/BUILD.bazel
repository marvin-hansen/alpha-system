load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "tests",
    srcs = glob([
        "*_tests.rs",
    ]),
    tags = [
        "specs_utils",
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Test target
        "//alias:service_specs_all",
        # Internal dependencies
    ],
)
