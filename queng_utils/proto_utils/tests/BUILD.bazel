load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "tests",
    srcs = glob([
        "*_tests.rs",
    ]),
    tags = [
        "unit",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate under test
        "//:proto_utils",
        # Internal crates
        "//:common",
        "//:rust_proto",
    ],
)
