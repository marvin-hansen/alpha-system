load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "mod",
    srcs = ["mod.rs"],
    deps = [
        "//queng_common/src:common",
        "@crates//:chrono",
        "@crates//:csv",
        "@crates//:encoding_rs",
        "@crates//:rust_decimal",
    ],
)
