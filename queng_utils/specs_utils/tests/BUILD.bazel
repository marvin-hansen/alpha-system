load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "specs_utils_tests",
    srcs = glob([
        "*_tests.rs",
    ]),
    tags = [
        "specs_utils",
        "unit",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Test target
        "//alias:specs_utils",
        # Internal dependencies

    ],
)
