# Test Utils

## Docker Utils

Docker utils help to create, re-use, or stop containers during tests. Requires 
a Docker installation with a working CLI. 

For an usage example, see [src_docker_bin](src/docker_bin). 