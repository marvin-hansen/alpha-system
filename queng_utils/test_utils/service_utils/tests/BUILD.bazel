load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "service_utils_test",
    srcs = glob([
        "*_tests.rs",
    ]),
    tags = [
        "service_utils_test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate under test
        "//alias:service_utils",
        # Internal crates
        "//alias:common_config",
        # External crates
    ],
)
