load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "crypto_utils_tests",
    srcs = glob([
        "*_tests.rs",
    ]),
    tags = [
        "crypto_utils_tests",
        "unit",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate under test
        "//:crypto_utils",
    ],
)
