load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "kaiko_utils_tests",
    size = "medium",
    srcs = glob([
        "*_tests.rs",
    ]),
    exec_properties = {
        # Tell BuildBuddy to run this test using a Firecracker microVM.
        "test.workload-isolation-type": "firecracker",
        # Tell BuildBuddy to ensure that the Docker daemon is started
        "test.init-dockerd": "true",
        # Tell BuildBuddy to preserve the microVM state across test runs.
        "test.recycle-runner": "true",
        # We want different different tests to be assigned to the same recycled runner,
        "test.runner-recycling-key": "kaiko_utils",
    },
    tags = [
        "kaiko_utils_integration_tests",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate under test
        "//alias:kaiko_utils",
        # Internal crates
        "//alias:env_utils",
        "//alias:kaiko_client",
        # External crates
        "@crates//:tokio",
    ],
)
