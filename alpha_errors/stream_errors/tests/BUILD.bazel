load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "stream_errors",
    srcs = glob([
        "*_tests.rs",
    ]),
    tags = [
        "stream_errors",
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:stream_errors",
    ],
)
