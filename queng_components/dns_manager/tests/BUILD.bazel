load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "tests",
    srcs = glob([
        "*_tests.rs",
    ]),
    tags = [
        "components",
        "dns_manager",
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate under test
        "//alias:dns_manager",
        # Internal crates
        "//alias:common_env",
        "//alias:ctx_manager",
        # External crates
        "//thirdparty/crates:tokio",
    ],
)
