load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "dns_manager_tests",
    srcs = glob([
        "*_tests.rs",
    ]),
    tags = [
        "components",
        "dns_manager",
        "unit",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate under test
        "//alias:dns_manager",
        "//alias:common_config",
        # Internal crates
        "//alias:ctx_manager",
    ],
)
