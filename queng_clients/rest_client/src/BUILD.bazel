load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "lib",
    srcs = ["lib.rs"],
    deps = [
        "@crates//:anyhow",
        "@crates//:reqwest",
        "@crates//:serde",
    ],
)
