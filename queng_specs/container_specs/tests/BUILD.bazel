load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "container_specs_tests",
    srcs = glob([
        "*_tests.rs",
    ]),
    tags = [
        "specs",
        "unit",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:container_specs",
        # Internal crates
    ],
)
