load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "vex_tests",
    srcs = glob([
        "*_tests.rs",
    ]),
    tags = [
        "specs",
        "unit",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Test target
        "//:vex_specs",
        # Internal dependencies
        "//:common",
    ],
)
