load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test_suite")
load("@rules_rust//rust:defs.bzl", "rust_doc", "rust_doc_test")

rust_library(
    name = "cmdb_specs",
    srcs = glob([
        "src/lib.rs",
    ]),
    deps = [
        # Internal dependencies
        "//:common",
    ],
    tags = ["specs", "service_specs", "cmdb_specs"],
    visibility = ["//visibility:public"],
)

rust_doc(
    name = "doc",
    crate = ":cmdb_specs",
    tags = ["doc"],
    visibility = ["//visibility:public"],
)

rust_doc_test(
     name = "doc_test",
     crate = ":cmdb_specs",
     tags = ["doc-test"],
     visibility = ["//visibility:public"],
)

rust_test_suite(
    name = "cmdb_tests",
    srcs = glob([
        "tests/*_tests.rs",
    ]),
    deps =  [
        # Test target
        ":cmdb_specs",
        # Internal dependencies
        "//:common",
    ],
    tags = [
        "specs",
        "unit",
    ],
    visibility = ["//visibility:public"],
)