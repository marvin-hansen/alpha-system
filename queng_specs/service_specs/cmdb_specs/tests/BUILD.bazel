load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "cmdb_tests",
    srcs = glob([
        "*_tests.rs",
    ]),
    tags = [
        "specs",
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Test target
        "//alias:cmdb_specs",
        # Internal dependencies
        "//alias:common_config",
    ],
)
