load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "qdgw_tests",
    srcs = glob([
        "*_tests.rs",
    ]),
    tags = [
        "specs",
        "unit",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Test target
        "//:qdgw_specs",
        # Internal dependencies
        "//:common",
    ],
)
