actions:
  - name: "Build and test all targets"
    container_image: ubuntu-20.04
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
    bazel_commands:
      - "build -c opt //..."
      - "test -c opt  //..."

  - name: "Build all service binaries"
    container_image: ubuntu-20.04
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
    bazel_commands:
      - "build -c opt //queng_services:build"

  - name: "Build all docs"
    container_image: ubuntu-20.04
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
    bazel_commands:
      - "build -c opt //:doc"

  - name: "Build and publish all images"
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
    bazel_commands:
      - "run   -c opt //:buildbuddy_setup_docker_auth --verbose_failures"
      - "build -c opt //:build"
      - "build -c opt //:image"
      - "run   -c opt //:push --workspace_status_command 'tools/workspace_status.sh'"