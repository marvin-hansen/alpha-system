load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "ring_buffer",
    srcs = glob([
        "ring_buffer/*_tests.rs",
    ]),
    tags = [
        "alpha_socket",
        "ring_buffer",
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:alpha_socket",
        # Internal dependencies

        # External dependencies
        "//thirdparty/crates:tempfile",
    ],
)

rust_test_suite(
    name = "socket",
    srcs = glob([
        "socket/*_tests.rs",
    ]),
    tags = [
        # Disabled for CI testing for the time being.
        # "alpha_socket",
        # "socket",
        # "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//alias:alpha_socket",
        # Internal dependencies

        # External dependencies
        "//thirdparty/crates:tempfile",
    ],
)
